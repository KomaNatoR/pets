import { counterInitState } from "./counter/initialState";
import { todoInitState } from "./todo/initialState";

export const initialState = {
    counter: counterInitState,
    todo: todoInitState,
};