// тут прописуються стилі для кнопок навігації в НОТІСАХ!

// НЕДОРОБЛЕНА!!!

export const noticesNavButt = ({ colors }) => `
    padding: 8px 16px;

    font-family: inherit;
    font-weight: 500;
    font-size: 14px;
    line-height: calc(19 / 14);
    letter-spacing: 0.04em;

    border: none;
    border-radius: 40px;
    color: inherit;
    background: inherit;
`;